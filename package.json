{
  "name": "boatz",
  "version": "0.1.0",
  "description": "A deep economic simulation of a pirate-age archipelago: hundreds of islands, ships, captains and crews, all running on their own decisions. Server-authoritative, deterministic, no build step. Built on Pat_Engine.",
  "license": "MIT",
  "author": "Patrick <prdoring@gmail.com>",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node server/main.js",
    "dev": "node --watch server/main.js",
    "test": "node --import ./tests/helpers/register-loader.js --test --test-reporter spec tests/*.test.js",
    "test:dot": "node --import ./tests/helpers/register-loader.js --test --test-reporter dot tests/*.test.js",
    "smoke": "node scripts/browser-smoke.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
